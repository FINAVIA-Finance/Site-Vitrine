import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Partenaires outils finance et IA",
    description: "Découvrez l'écosystème de partenaires Finavia pour digitaliser, automatiser et piloter la fonction finance avec les meilleurs outils du marché.",
};

export default function PartenairesLayout({ children }: { children: React.ReactNode }) {
    return children;
}
